#include <bits/stdc++.h>
using namespace std;
int main(){
    int N, M;
    vector<vector<int>> queues;
    cin >> N >> M;
    for (int i = 0; i < N; i++)
    {
        int mode, time_quantum;
        vector<int> q;
        cin >> mode >> time_quantum;
        q.push_back(mode);
        q.push_back(time_quantum);
        queues.push_back(q);
    }
    for (int i = 0; i < M; i++)
    {
        
    }
    
    
}